{
  "version": 2,
  "builds": [
    { "src": "api/index.js", "use": "@vercel/node" } // Adjust "index.js" if needed
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" } // Route all requests to your worker
  ]
}
